{% trans_default_domain 'users' %}
{% extends 'admin/base.html.twig' %}
{% set page_title = 'groups.listing.title' | trans %}

{% block pageActions %}
    <div class="page-actions">
        <a href="{{ path('admin_users_group_new') }}" class="btn-primary">
            <i class="fas fa-plus"></i> Nouveau
        </a>
    </div>
{% endblock %}

{% block pageContent %}
        {{ component('GroupList') }}
{% endblock %}

{% block javascripts %}
    {{ importmap(['app']) }}
{% endblock %}